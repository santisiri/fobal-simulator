{
  "name": "@fobal/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authoritative headless FOBAL match engine. Wraps the golden simulation core hermetically (no DOM/Canvas/window/localStorage) behind a MatchManifest adapter with external ids, single-point rating normalization, command scheduling and deterministic hashing.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fobal/protocol": "*"
  }
}
